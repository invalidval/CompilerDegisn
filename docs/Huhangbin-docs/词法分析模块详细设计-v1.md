# 词法分析模块详细设计-v1

- 版本：v1.1.0
- 日期：2026-03-21
- 负责人：成员2（词法分析）
- 实现文件：src/lexer.l、src/main.cpp、src/parser.y

## 1. 模块目标

1. 将 Pascal-S 源码切分为稳定 Token 流。
2. 正确维护 1-based 行列号。
3. 对词法错误进行定位与上报，并支持继续扫描。
4. 为联调提供词法独立输出能力（Type, Lexeme, Line, Column）。

## 2. 输入与输出

1. 输入：字符流（yyin 指向 .pas 文件）。
2. 输出：
- 语法模式：返回 Bison token（关键字 token、IDENTIFIER、NUMBER、CHARACTER、STRING、多字符运算符 token 与单字符 token）。
- 联调模式（--lex）：输出 Type、Lexeme、Line、Column；出错时输出错误清单并返回非 0。

## 3. 词法规则

### 3.1 关键字与标识符

1. 标识符规则：`[a-zA-Z_][a-zA-Z0-9_]*`。
2. 关键字比较：大小写不敏感（先 lower 再匹配关键字集合）。
3. 关键字集合：program、const、var、integer、real、boolean、char、array、of、function、procedure、begin、end、if、then、else、while、do、for、to、downto、read、write、not、and、or、div、mod。
4. 返回策略：关键字返回对应关键字 token；非关键字返回 IDENTIFIER，且 yylval.text 存储小写化后的标识符。

### 3.2 常量与符号

1. 整数：`[0-9]+`。
2. 实数：`[0-9]+\.[0-9]+([eE][+-]?[0-9]+)?`。
3. 前导小数点实数：`.5` 形式。
4. 科学计数法：`1e10`、`2E-3` 形式。
5. 十六进制：`$FF`、`0x1A` 形式。
6. 字符常量：`'c'` 与转义形式（如 `\n`）。
7. 字符串常量：`"..."`，支持常见转义。
8. 多字符运算符：`:=`、`<=`、`>=`、`<>`、`..`。
9. 单字符运算符：`+ - * / = < >`。
10. 分隔符：`: ; , . ( ) [ ]`。

### 3.3 注释、预编译指令与空白

1. 注释采用独立状态机 COMMENT，支持 `{...}`、跨行与嵌套（深度计数）。
2. 预编译指令采用独立状态机 DIRECTIVE，支持 `{$...}` 跳过。
3. 注释与指令均不产出 token，但会更新行列号。
4. 空白字符（空格、制表、换行）均参与位置推进。

### 3.4 token 与语义值

1. IDENTIFIER、NUMBER、CHARACTER、STRING 使用 yylval.text 传递词素文本。
2. 关键字、分隔符、运算符通常不附加 yylval。
3. 多字符运算符返回 ASSIGN、LE、GE、NE、DOTDOT。

## 4. 错误处理协议

1. 错误格式：`Lexical error: <reason> (lexeme='<text>')`。
2. 错误类别：
- illegal character
- malformed number
- malformed character literal
- unterminated character literal
- unterminated string literal
- unterminated comment
- unterminated directive
3. 恢复策略：记录错误后继续扫描，保证可达 EOF。

## 5. 位置统计规则

1. 行列号从 1 开始。
2. 记录 token 首字符位置。
3. 每条规则触发时先固化 token 起点，再推进当前位置（YY_USER_ACTION）。
4. 注释/指令状态中遇到换行，同样推进行列，保证后续 token 定位准确。

## 6. 对外接口

1. `lexerResetState()`：清空词法状态。
2. `lexerLastLexeme()` / `lexerLastType()` / `lexerLastLine()` / `lexerLastColumn()`：提供最近 token 元信息。
3. `lexerClearErrors()` / `lexerErrorCount()` / `lexerErrorAt(i)`：提供词法错误集合访问。
4. `--lex` 模式输出由 src/main.cpp 实现，供测试脚本做稳定断言。

## 7. 测试设计

1. 回归样例矩阵：词法测试矩阵-v1.md（当前版本 v1.7.0，含回归结果）。
2. 自动化单测脚本：test/test_lexer_unit.sh。
3. 当前样例规模：valid 28、invalid 14。
4. 覆盖点：关键字大小写、复杂标识符、科学计数法、前导小数点、十六进制、字符与字符串常量、注释（跨行+嵌套）、预编译指令、非法字符、格式错误数字、未闭合结构（注释/字符串/指令/字符）。

## 8. 已知边界

1. 当前详细设计聚焦词法阶段，token 的语法消费路径由 parser/semantic 阶段继续实现。
2. 若后续新增 Pascal-S 子集关键字或符号，需同步更新关键字集合与测试矩阵并升版文档。
