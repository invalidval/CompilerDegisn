# 词法契约-v1

- 版本号：Lexical-Contract v1.1.0
- 修订日期：2026-03-21
- 适用范围：Pascal-S 编译器词法分析模块
- 对照基准：docs/需求分析(1).md（第 2.1 源语言规范、第 3.1 词法分析器需求）
- 实现基线：include/token.h、include/error_handler.h、src/lexer.l、src/main.cpp

## 1. 目标与边界

1. 本契约定义词法分析器的输入、输出、错误行为与验收口径。
2. 本契约仅覆盖词法层，不约束语法与语义阶段的产生式消费细节。
3. 与需求分析不冲突时，允许实现扩展能力，但必须在文档中标注为“扩展项”。

## 2. 严格对照需求分析（词法）

| 需求条目（需求分析） | 契约要求 | 状态 |
|---|---|---|
| 标识符：letter (letter\|digit)*；大小写不敏感 | 接受字母或下划线开头、后续字母数字下划线；关键字比较与符号表查找按小写归一化 | 必选 |
| 关键字集合（program/begin/end/.../div/mod） | 关键字大小写不敏感；关键字优先于标识符 | 必选 |
| 常数：整数、实数、字符常量 | 支持整数/实数/字符常量识别与语义值通道 | 必选 |
| 运算符：:=, =, <>, <, <=, >, >=, +, -, *, /, div, mod, and, or, not | 识别全部列出符号与关键字运算符 | 必选 |
| 分隔符与注释：空白、{...} 注释 | 注释不产出 token；行列号持续累计 | 必选 |
| 词法错误：非法字符、未闭合注释、非法数字、标识符超长 | 统一上报并尽量恢复扫描至 EOF | 必选 |
| 标识符长度建议 8 | 当前实现不强制 8（采用更宽上限），属于可配置策略 | 建议项 |

说明：需求分析中“关键字前后必须有空格或换行”由词法切分天然满足，不额外施加硬编码限制。

## 3. Token 分类与命名

### 3.1 逻辑分类

1. Keyword
2. Identifier
3. Number
4. Character
5. Operator
6. Delimiter
7. EndOfFile
8. Invalid

### 3.2 返回命名（Bison token）

1. 关键字：全大写符号（如 PROGRAM、CONST、VAR）。
2. 通用词素：IDENTIFIER、NUMBER、CHARACTER、STRING（扩展项）。
3. 单字符符号：可直接返回字符字面值（如 ';'）。

## 4. 大小写与语义值约定

1. 关键字：大小写不敏感识别。
2. 标识符：用于比较时转小写；输出可保留原词素用于调试展示。
3. IDENTIFIER/NUMBER/CHARACTER 的语义值通过 yylval 通道提供。
4. 行号、列号均采用 1-based，记录 token 起始位置。

## 5. 行列号与注释规则

1. 空格列号 +1；制表符按 1 列处理；换行行号 +1 且列号重置为 1。
2. 注释 `{...}` 内部不产出 token，但必须持续更新行列号。
3. 多字符 token 记录起始位置，扫描后推进当前位置。
4. EOF 时若存在未闭合注释，必须报错并安全结束扫描。

## 6. 错误上报协议

### 6.1 接口

使用 include/error_handler.h 的统一接口：
1. report(line, column, message)
2. hasErrors()
3. errors()
4. clear()

### 6.2 消息格式

推荐格式：Lexical error: <reason> (lexeme='<text>')

reason 最少覆盖：
1. illegal character
2. malformed number
3. unterminated character literal
4. unterminated comment

### 6.3 恢复策略

1. 非法单字符：报错后跳过，继续扫描。
2. 格式错误数字：报错后最小前进，避免死循环。
3. 未闭合结构：在 EOF 报错并终止扫描。
4. 无论是否报错，扫描过程应可达 EOF。

## 7. 与 parser 的兼容边界

1. 必须稳定支持 PROGRAM IDENTIFIER ';' 基础链路。
2. IDENTIFIER 的语义值需满足 parser 端可释放约定。
3. 词法增强不得破坏现有最小语法通路。

## 8. 扩展项声明（不影响基础需求验收）

以下能力属于实现增强，不作为需求分析的硬性最低线：
1. 字符串常量识别（STRING）。
2. 十六进制数字（$FF、0x1A）。
3. 嵌套注释处理。
4. 预编译指令（{$...}）识别与错误检测。

## 9. 版本冻结声明

本文件为 v1.1.0 冻结契约。若后续变更，需新增版本文件并附变更说明，不直接覆盖旧版口径。
