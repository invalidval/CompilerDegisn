#include <stdio.h>

int a, b, c, d, e;
int flag;

int main(void) {
a = 1;
b = 0;
c = 1;
d = 2;
e = 4;
flag = false;
if ((((((a * b) / c) = (e + d)) and (((a * (a + b)) + c) <= (d + e))) or ((a - (b * c)) = (d - (a / c))))) {
        flag = true;
    }
if (flag) {
        printf("%d", 1);
    }

    return 0;
}
