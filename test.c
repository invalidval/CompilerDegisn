#include <stdio.h>

int main(void) {
    const int t = "haha";
    const double a = 1e6;
    int x, int y;
    int z[10];
    int u;
int gcd(int a, int b) {
    int _retval;
if ((b = 0)) {
        _retval = a;
    } else {
        _retval = gcd(b, (a mod b));
    }

    return _retval;
}

    z[2][3] = 5;
u = 1;
scanf("%d%d", &x, &y);
printf("%d", ASTNode);
    input, output

    return 0;
}
